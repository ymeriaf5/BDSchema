package com.example.schemamaker.repositories;

import com.example.schemamaker.entities.Couverture;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CouvertureRepository extends JpaRepository<Couverture,Integer> {
}
